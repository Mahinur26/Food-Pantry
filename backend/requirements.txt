fastapi==0.104.1
uvicorn==0.24.0
firebase-admin==6.2.0
google-cloud-aiplatform==1.70.0
pydantic==2.5.0
python-dotenv==1.0.0
# Install numpy FIRST (before PyTorch) - required by PyTorch and transformers
# PyTorch 2.2.0 requires numpy < 2.0
numpy>=1.24.0,<2.0.0
pillow>=9.0.0
# CPU-only PyTorch (no CUDA dependencies, saves ~1GB+ memory)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu
torchvision==0.17.0+cpu
transformers>=4.30.0
python-multipart==0.0.20